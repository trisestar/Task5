/*
package factory;

import entity.Cone;
import entity.Point;

public class ConeFactory {
    public Cone createCone(long id, double[] params) {
        Point point = new Point(params[0], params[1], params[2]);
        return new Cone(id, point, params[3], params[4]);
    }
}
*/
//todo